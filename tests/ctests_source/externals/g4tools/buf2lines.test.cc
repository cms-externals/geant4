// ********************************************************************
// * License and Disclaimer                                           *
// *                                                                  *
// * The  Geant4 software  is  copyright of the Copyright Holders  of *
// * the Geant4 Collaboration.  It is provided  under  the terms  and *
// * conditions of the Geant4 Software License,  included in the file *
// * LICENSE and available at  http://cern.ch/geant4/license .  These *
// * include a list of copyright holders.                             *
// *                                                                  *
// * Neither the authors of this software system, nor their employing *
// * institutes,nor the agencies providing financial support for this *
// * work  make  any representation or  warranty, express or implied, *
// * regarding  this  software system or assume any liability for its *
// * use.  Please see the license in the file  LICENSE  and URL above *
// * for the full disclaimer and the limitation of liability.         *
// *                                                                  *
// * This  code  implementation is the result of  the  scientific and *
// * technical work of the GEANT4 collaboration.                      *
// * By using,  copying,  modifying or  distributing the software (or *
// * any work based  on the software)  you  agree  to acknowledge its *
// * use  in  resulting  scientific  publications,  and indicate your *
// * acceptance of all terms of the Geant4 Software license.          *
// ********************************************************************
//
//---------------------------------------------------------------------------//
// Unit test for tools/buf2lines
// Initial implementation generated by GitHub CoPilot (GPT-4.1)
//---------------------------------------------------------------------------//
#include "tools/buf2lines"

#include <gtest/gtest.h>

namespace tools
{
//---------------------------------------------------------------------------//
TEST(Buf2Lines, RoundTripBasic)
{
  std::vector<std::string> input = {"hello", "world", "foo", "bar"};
  size_t buf_size = 0;
  char* buffer = nullptr;
  ASSERT_TRUE(strings2buf(input, buf_size, buffer));
  ASSERT_NE(buffer, nullptr);

  std::vector<std::string> output;
  ASSERT_TRUE(buf2strings(buf_size, buffer, output));
  EXPECT_EQ(input, output);

  delete[] buffer;
}

TEST(Buf2Lines, EmptyString)
{
  std::vector<std::string> input = {""};
  size_t buf_size = 0;
  char* buffer = nullptr;
  ASSERT_TRUE(strings2buf(input, buf_size, buffer));
  ASSERT_NE(buffer, nullptr);

  std::vector<std::string> output;
  ASSERT_TRUE(buf2strings(buf_size, buffer, output));
  EXPECT_EQ(input, output);

  delete[] buffer;
}

TEST(Buf2Lines, EmptyVector)
{
  std::vector<std::string> input;
  size_t buf_size = 0;
  char* buffer = nullptr;
  ASSERT_TRUE(strings2buf(input, buf_size, buffer));
  ASSERT_NE(buffer, nullptr);

  std::vector<std::string> output;
  // buf2strings should fail for size <= 1
  EXPECT_FALSE(buf2strings(buf_size, buffer, output));
  delete[] buffer;
}

TEST(Buf2Lines, EmbeddedNulls)
{
  // This test is expected to fail because the buffer format does not support embedded nulls.
  // e.g:
  //     std::vector<std::string> input = {"a\0b", "c\0d", std::string("e\0f", 3)};
  // If we want to support the above, implement the test for it here!
  GTEST_SKIP() << "Buffers with embedded nulls not yet supported";
}

TEST(Buf2Lines, SingleString)
{
  std::vector<std::string> input = {"single"};
  size_t buf_size = 0;
  char* buffer = nullptr;
  ASSERT_TRUE(strings2buf(input, buf_size, buffer));
  ASSERT_NE(buffer, nullptr);

  std::vector<std::string> output;
  ASSERT_TRUE(buf2strings(buf_size, buffer, output));
  EXPECT_EQ(input, output);

  delete[] buffer;
}

TEST(Buf2Lines, MultipleEmptyStrings)
{
  std::vector<std::string> input = {"", "", ""};
  size_t buf_size = 0;
  char* buffer = nullptr;
  ASSERT_TRUE(strings2buf(input, buf_size, buffer));
  ASSERT_NE(buffer, nullptr);

  std::vector<std::string> output;
  ASSERT_TRUE(buf2strings(buf_size, buffer, output));
  EXPECT_EQ(input, output);

  delete[] buffer;
}

//---------------------------------------------------------------------------//
}  // namespace tools
